# 是否使用Hash路由
VITE_USE_HASH = 'true'

# 资源公共路径,需要以 /开头和结尾
VITE_PUBLIC_PATH = '/'

# 生产环境 - 嵌入到 Rust 中，使用相对路径
VITE_AXIOS_BASE_URL = '/'  # 用于同域 API 请求

# 代理配置-target（生产环境不使用，但保留配置）
# VITE_PROXY_TARGET = 'http://localhost:8080'
